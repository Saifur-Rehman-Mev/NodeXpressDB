<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit your chats</title>
</head>
<body>
    <h2> Editor : <%= chat._id %></h2>
    <p> this chat is send by <b><%= chat.from %></b> to <b><%= chat.to %></b></p>
    <form method="POST" action="/chats/<%= chat._id %>?_method=PUT">
        <textarea name="message"> <%= chat.message %> </textarea>
        <button> Edit </button>
    </form>
 
</body>
</html>